@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/ctyprTargets.cmake")

check_required_components(ctypr)

# Path to content DB pipeline scripts (for building words.db / sentences.db)
set_and_check(CTYPR_CONTENT_DB_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../share/ctypr/content-db")