name: Release

on:
  push:
    tags: ['v*']

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        config: [Release]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=${{ matrix.config }} -DBUILD_TESTING=ON -DBUILD_EXAMPLES=OFF

      - name: Build
        run: cmake --build build --config ${{ matrix.config }}

      - name: Test
        working-directory: build
        run: ctest --output-on-failure -C ${{ matrix.config }}

      - name: Package
        run: cmake --build build --config ${{ matrix.config }} --target package

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ctypr-${{ github.ref_name }}-${{ matrix.os }}
          path: |
            build/ctypr-*.tar.gz
            build/ctypr-*.zip
          if-no-files-found: warn

  build-db:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r tools/content-db/requirements.txt

      - name: Build content databases
        run: python tools/content-db/run_pipeline.py --sentences-db sentences.db --words-db words.db

      - name: Upload DB artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ctypr-${{ github.ref_name }}-content-db
          path: |
            sentences.db
            words.db

  release:
    needs: [build, build-db]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: ctypr-${{ github.ref_name }}-*
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            ctypr-*.tar.gz
            ctypr-*.zip
            sentences.db
            words.db
